
export interface ComponentProps {
    email: string;
    onClose: () => void;
}

export interface codeValidProps{
    email:string,
    code: string,
    onClose:()=>void,
}
export interface CodeValidTimeProps{
    onClose:()=>void,
}